# Goose config for Mac Mini M1 orchestrator.
# Copy to ~/.config/goose/config.yaml

# Provider: Ollama on RTX server via Tailscale
GOOSE_PROVIDER: ollama
GOOSE_MODEL: qwen2.5:7b
OLLAMA_HOST: http://milai:11434

# Fallback: uncomment + set ANTHROPIC_API_KEY in keychain if Ollama is down
# GOOSE_PROVIDER: anthropic
# GOOSE_MODEL: claude-haiku-4-5-20251001
# ANTHROPIC_API_KEY is read by Goose from env; load from macOS Keychain in bootstrap.sh
